{
  "name": "chart_canvas",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "tsc --watch",
    "serve": "live-server --open=./ --port=8081",
    "dev": "npm-run-all --parallel watch serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.6.3"
  }
}
